[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
uuid = { version = "1.0", features = ["serde", "v4"] }
chrono = { workspace = true }
sqlx = { version = "0.8", features = ["postgres", "macros", "chrono", "uuid"] }
