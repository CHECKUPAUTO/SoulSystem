[package]
name = "soullink-cutlass"
version = "0.1.0"
edition = "2021"

[features]
default = ["cutlass"]
cutlass = ["dep:cust"]

[dependencies]
thiserror = "1.0"
tracing = "0.1"
cust = { version = "0.3", optional = true }

[build-dependencies]
cc = "1.0"