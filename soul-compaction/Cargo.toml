[package]
name = "soul_compaction"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "4-pass context compaction: Reclaim → Shrink → Collapse → Evict"

[dependencies]
serde = { workspace = true }
serde_json = "1"
tokio = { workspace = true }
thiserror = "2"
tracing = "0.1"
uuid = { version = "1", features = ["v4"] }
chrono = { workspace = true }
regex = "1"

[dev-dependencies]
tempfile = "3"
tokio = { workspace = true }
