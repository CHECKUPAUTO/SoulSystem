--- a/crates/avid-orchestrator/src/pipeline.rs
+++ b/crates/avid-orchestrator/src/pipeline.rs
@@ -15,9 +15,10 @@ use avid_vision::{UIComponent, VisionEngine};
 /// Retry an async operation with exponential backoff.
 ///
 /// `max_retries` is the maximum number of *additional* attempts after the first failure.
-async fn with_retry<T, F, Fut>(operation: F, max_retries: u32) -> Result<T, CoreError>
n+async fn with_retry<T, E, F, Fut>(operation: F, max_retries: u32) -> Result<T, CoreError>
 where
+    E: std::fmt::Display,
     F: Fn() -> Fut,
-    Fut: std::future::Future<Output = Result<T, CoreError>>,
+    Fut: std::future::Future<Output = Result<T, E>>,
 {
     let mut last_err = None;
     for attempt in 0..=max_retries {
@@ -26,7 +27,7 @@ where
         }
         match operation().await {
             Ok(val) => return Ok(val),
-            Err(e) => last_err = Some(e),
+            Err(e) => last_err = Some(CoreError::Agent(e.to_string())),
         }
     }
     Err(last_err.unwrap_or_else(|| CoreError::Agent("exhausted retries".into())))
