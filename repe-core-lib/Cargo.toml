[package]
name = "repe_core_lib"
version = "0.1.0"
edition = "2021"

[lib]
name = "repe_core_lib"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
# Local dependency to scirust core for matrix views and kernels
scirust-core = { path = "/root/scirust/scirust-core" }

[build-dependencies]
# If needed, otherwise maturin handles the build
