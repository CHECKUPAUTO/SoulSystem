[package]
name = "soul_matrix_engine"
version = "0.1.0"
edition = "2021"
description = "SIMD-optimized GEMM kernel with runtime dispatch (AVX-512, AVX2, NEON) and cache-conscious tiling via HardwareManifest"
license = "MIT"
repository = "https://github.com/CHECKUPAUTO/OS-AGENTS"
keywords = ["gemm", "simd", "avx512", "neon", "matrix", "linear-algebra"]
categories = ["math", "simd", "embedded", "science"]

[dependencies]
soul_scheduler = { path = "../soul_scheduler" }
