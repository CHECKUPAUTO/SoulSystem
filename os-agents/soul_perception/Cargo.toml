[package]
name = "soul_perception"
version = "0.1.0"
edition = "2021"
description = "Zero-copy signal parser converting DATA_/ERR_ tokens into typed IPC messages for agent perception pipeline"
license = "MIT"
repository = "https://github.com/CHECKUPAUTO/OS-AGENTS"
keywords = ["perception", "parser", "zero-copy", "token", "ipc", "signal"]
categories = ["embedded", "parser", "os"]

[dependencies]
soul_ipc = { path = "../soul_ipc" }
