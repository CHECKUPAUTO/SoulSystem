[package]
name = "ecosystem_synapse_linker"
version = "0.1.0"
edition = "2021"
description = "Ecosystem-wide synapse linking for cross-agent neural pathway formation and dynamic topology rewiring"
license = "MIT"
repository = "https://github.com/CHECKUPAUTO/OS-AGENTS"
keywords = ["synapse", "linker", "ecosystem", "neural", "topology", "rewiring", "plasticity"]
categories = ["machine-learning", "neuroscience", "embedded", "distributed-systems"]

[dependencies]
parking_lot = { workspace = true }
