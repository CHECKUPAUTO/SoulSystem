[package]
name = "soul-core"
version.workspace = true
edition.workspace = true
description = "Core traits and shared types for SoulNeural System 3"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
