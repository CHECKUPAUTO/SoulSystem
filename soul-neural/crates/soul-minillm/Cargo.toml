[package]
name = "soul-minillm"
version.workspace = true
edition.workspace = true
description = "Char-level transformer for local inference"

[dependencies]
scirust-core = { workspace = true }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
rand = "0.8"
tracing = "0.1"
lazy_static = "1"
prometheus = { version = "0.13", features = ["process"] }
log = "0.4"
