[package]
name = "soul-rstdp"
version.workspace = true
edition.workspace = true
description = "R-STDP optimizer with Dual numbers (neuro-evolutionary)"

[dependencies]
scirust-autodiff = { workspace = true }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
