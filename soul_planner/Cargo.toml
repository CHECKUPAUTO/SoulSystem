[package]
name = "soul_planner"
version = "0.1.0"
edition = "2021"
description = "Boucle cognitive (observe → planifie → agit → évalue → décide)"

[dependencies]
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
