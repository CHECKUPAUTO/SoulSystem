# IronReview Integration Opportunities

**Generated:** 2026-04-11 from night_cycle analysis  
**Scope:** Cross-repository ecosystem improvements

## Overview

Analysis of recent OpenClaw commits reveals patterns that could benefit from IronReview-style automated code review. This document maps detected patterns to IronReview v4 capabilities.

## Detected Patterns for Automation

### 1. Plugin Import Pattern Detection
**Pattern:** Direct registry imports vs barrel index imports

**Auto-Detect Criteria:**
```typescript
// Flag: Import from plugin index
import { X } from "../../plugins/index.js";

// Suggest: Import from registry
import { X } from "../../registry.js";
```

**IronReview Rule:**
- Severity: suggestion
- Condition: Import path ends with `/plugins/index.js`
- Message: "Consider direct registry import for performance"
- Reference: plugin_avoidance_pattern_2026-04-11.md

### 2. Static Capability Table Drift
**Pattern:** Pre-computed lookup tables may become stale

**Auto-Detect Criteria:**
```typescript
// Flag: Static table not referenced in tests
const STATIC_X_CAPABILITIES = { ... };
// Missing: Sync validation with plugin registry
```

**IronReview Rule:**
- Severity: warning
- Condition: File contains `STATIC_.*_CAPABILITIES` without corresponding test
- Message: "Add CI check to validate static table sync"

### 3. Test Mock Duplication
**Pattern:** Multiple independent mocks of same function

**Evidence:** Commits show repeated mocking of `normalizeChannelId` across test files

**IronReview Rule:**
- Severity: suggestion
- Condition: Same mock pattern in >3 test files
- Message: "Consider extracting shared test fixture"

## Repository-Specific Opportunities

### OpenClaw/OpenClaw
| Pattern | Frequency | Automation Priority |
|---------|-----------|---------------------|
| Plugin avoidance | 6 commits | High |
| Test narrowing | 7 commits | Medium |
| Static short-circuit | 2 commits | High |

### The Well Integration
**Opportunity:** Create IronReview-style dataset validation

```python
# Hypothetical: Dataset quality rules
class DatasetReview:
    def check_hdf5_security(self, file_path):
        """Flag potential CVE-2023-xx vulnerabilities"""
        
    def validate_tensor_shapes(self, dataset):
        """Auto-detect shape mismatches"""
```

### VisionClaw Integration
**Opportunity:** Security-focused IronReview rules

| Rule | Severity | Trigger |
|------|----------|---------|
| Hardcoded API keys | critical | `static let.*= "` pattern in Secrets.swift |
| HTTP URL usage | high | `http://` in network configuration |
| Missing retry logic | medium | Network calls without error handling |

## Recommended IronReview Rules (T430 Algorithm)

### Phase 1: Pattern Detection
1. **Import Path Analysis**
   - Detect barrel imports that could be registry imports
   - Score: path depth + frequency

2. **Static Table Analysis**
   - Find pre-computed lookup tables
   - Check for corresponding validation tests

3. **Mock Consolidation**
   - Identify duplicate mock patterns
   - Suggest shared fixture extraction

### Phase 2: Performance Scoring
```typescript
interface PerformanceScore {
  barrelImportCost: number;      // ms per import
  registryImportCost: number;    // ms per import
  estimatedSavings: number;      // (barrel - registry) * callCount
  confidence: number;            // 0-1 based on pattern match
}
```

### Phase 3: Remediation Generation
```typescript
// Auto-generated fix suggestion
const fix = {
  original: 'import { X } from "../../plugins/index.js";',
  suggested: 'import { X } from "../../registry.js";',
  rationale: 'Eliminates barrel export overhead',
  references: ['plugin_avoidance_pattern_2026-04-11.md'],
};
```

## Integration Architecture

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  OpenClaw Commits │────▶│  IronReview T430  │────▶│  Auto-PR/Comment │
└─────────────────┘     └──────────────────┘     └─────────────────┘
                               │
                               ▼
                        ┌──────────────────┐
                        │  Pattern DB       │
                        │  (this document)  │
                        └──────────────────┘
```

## References

- OpenClaw Commit e2d93fb5bc: perf: short-circuit static doctor channel capabilities
- OpenClaw Commit 455535a4f9: perf: avoid plugin index for target normalization
- IronReview v4: T430 phase-shift evolutionary algorithm
- Night Cycle Report: night_cycle_20260411_0745.md

## Action Items

- [ ] Implement plugin import detection rule
- [ ] Create static table validation CI check
- [ ] Design test fixture consolidation rule
- [ ] Integrate with OpenClaw PR workflow

---
*Document auto-generated by OpenEvolve Auto-Apply (safe: documentation only)*
