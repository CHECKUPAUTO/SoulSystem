[package]
name = "test_telemetry"
version = "0.1.0"
edition = "2021"

[dependencies]
soul_telemetry = { path = "../soul_telemetry" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time", "sync"] }