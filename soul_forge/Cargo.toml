[package]
name = "soul_forge"
version = "0.1.0"
edition = "2021"
description = "Memory allocator and object factory with telemetry integration for zero-allocation cognitive data structures"
license = "MIT"
repository = "https://github.com/CHECKUPAUTO/OS-AGENTS"
keywords = ["allocator", "factory", "zero-allocation", "telemetry", "memory", "pool"]
categories = ["memory-management", "embedded", "os"]

[dependencies]
soul_telemetry = { path = "../soul_telemetry" }