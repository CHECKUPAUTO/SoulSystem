/// Git-based sandbox for validating patches in isolation.
///
/// Currently a stub; will use `git worktree` in the future.
pub struct Sandbox;

impl Sandbox {
    /// Create a new sandbox instance.
    pub fn new() -> Self { Self }
}
