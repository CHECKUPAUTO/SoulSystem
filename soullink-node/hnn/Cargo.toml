[package]
name = "hnn"
version = "0.2.0"
edition = "2021"
description = "Hamiltonian Neural Network — symplectic Verlet integration, learned energy surface U(q) = α(q-μ)² + β(q-μ)⁴"

[dependencies]
ndarray = "0.16"
rand = "0.8"
scirust-core = { path = "/root/scirust/scirust-core" }
soullink-ssm = { path = "../soullink-ssm", optional = true }
