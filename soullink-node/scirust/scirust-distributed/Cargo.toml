[package]
name = "scirust-distributed"
version = "0.1.0"
edition = "2021"
description = "Distributed inference for SciRust: TCP-based model serving, data parallelism, rendezvous"

[dependencies]
scirust-inference = { path = "/root/scirust/scirust-inference" }
scirust-probability = { path = "/root/scirust/scirust-probability" }
