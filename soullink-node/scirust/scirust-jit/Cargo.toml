[package]
name = "scirust-jit"
version = "0.1.0"
edition = "2021"
description = "Runtime kernel JIT compilation for SciRust (template-based, no LLVM)"

[dependencies]
scirust-inference = { path = "/root/scirust/scirust-inference" }
