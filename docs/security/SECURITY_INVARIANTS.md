# SoulSystem Security Invariants

This document lists the invariants the production hardening effort must
establish and keep true. Each invariant is stated so that it can be checked by
a test or a reproducible command. An invariant is only considered **held** once
a test proves it; until then it is **target**.

Status legend: `HELD` (proven by a test in-tree) · `PARTIAL` (partially
enforced) · `TARGET` (not yet enforced).

The PR column refers to the [production hardening plan](PRODUCTION_HARDENING_PLAN.md)
PR sequence (A–P).

## Startup and mode

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-ENV-1 | The runtime mode is decided only from an explicit `SOULSYSTEM_ENV` value; production is never inferred from the absence of a flag. | A | HELD (`soul-prod-guard`: `mode_unset_is_error`, `mode_unknown_is_error`) |
| INV-ENV-2 | In production, any single unmet security prerequisite aborts startup before any listener opens (fail closed). | A | HELD (`soul-prod-guard`: per-violation rejection tests) |
| INV-ENV-3 | No secret value ever appears in a guard violation, audit event, or startup error. | A | HELD (`violation_display_carries_no_secret_values`, `rejected_error_message_is_stable_and_secret_free`) |

## Tool dispatch and execution

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-TOOL-1 | An unregistered tool name can only produce `UnknownTool`; it can never reach process execution. | B | HELD (`soul_tools`: `unknown_tool_never_executes`, `malformed_tool_names_rejected`) |
| INV-TOOL-2 | Every tool has a trusted, statically-registered capability that the caller cannot downgrade. | C | HELD (`soul_tools`: `write_tools_are_not_classified_as_read`, `capability_classification_is_trusted_and_correct`) |
| INV-EXEC-1 | No production-reachable code path invokes `std::process::Command` / `tokio::process::Command` / `libc::exec*` outside the single approved sandbox executor. | D/N | PARTIAL (`soul_tools::execute_shell` — the agent dispatch path, and the `execute_tool` helper that calls it — now route through `soul_sandbox`; `execute_shell_routes_through_sandbox` test. Within `soul_sandbox` itself, `execute_with_stdin` previously built its own `Command` with only `setpgid` in `pre_exec`, bypassing seccomp/network isolation entirely — fixed in D-2 by routing it through the same `apply_sandbox_pre_exec` helper as `execute()`; `execute_with_stdin_also_gets_network_isolation_when_the_host_permits_it`. N: `AutonomousAgent::executor` — an `AsyncShellExecutor` field constructed but never referenced (confirmed via `grep -n "self.executor" soul-agent-core/src/` returning zero matches before removal), sitting beside the real dispatch path and implying a second, unused sandboxing mechanism existed — removed entirely (field, import, both constructor sites); `run_react_dispatch_path_is_sandboxed_for_destructive_commands` calls the exact `async_dispatch_tool` function `run_react`'s tool-dispatch loop calls, not a mock, and asserts it refuses a destructive command end-to-end. **Re-verified at main 898f472 (see `findings.json` CRIT-001):** a workspace-wide inventory returns 227 process-execution matches in the live tree, 110 of them inside 25 workspace-member crates, and at least one is production-reachable and unsandboxed — `src/main.rs` instantiates `SelfHealer` during daemon startup ("Phase 5"), ungated, and spawns a bus subscriber that calls `SelfHealer::execute` on `error.*` events, which runs `kill`, `systemctl restart/start` and `df` via bare `std::process::Command`. `soul-automodify` invokes `cargo` and the `soul_gateway` iMessage provider invokes `osascript`, both also outside the sandbox. **P0-1 (this change):** the earlier note above overstated reachability and is corrected here — of the three spawn sites in `src/self_healer.rs`, only `df` was ever live (called every 30s from the spawned `run()` loop); `KillNonEssential`/`RestartService` have **no producer** in the workspace (`Preservation` emits only Throttle, MemoryDump, EmergencySave, LocalFallback, DistressSignal, GracefulShutdown), so those arms were latent rather than attacker-reachable. All three are now addressed: `df` is replaced by `statvfs(3)` (`SelfHealer::root_disk_used_percent`) so the spawn is removed rather than sandboxed; `kill(1)` is replaced by a direct `libc::kill` signal; both privileged arms are gated behind `ProcessControl::Enabled` (default `Disabled`); and the unit name is validated by `is_safe_service_name`. `tests/architecture_process_execution.rs` pins the permitted set of process-spawning files in the binary crate (`no_unapproved_process_execution_in_the_soulsystem_binary`), rejects stale allowlist entries (`every_allowlist_entry_is_still_justified`), and asserts disk telemetry never shells out again (`disk_telemetry_does_not_shell_out`); the guard's failure path was verified by introducing a deliberate violation. **P1-8:** the guard now covers **every workspace member**, not `src/` alone. Member directories are parsed from the root `Cargo.toml` rather than hardcoded, so adding a crate brings it under the guard automatically — a new crate should not have to be *remembered* into a security scan. All 30 production spawn files are classified with a category and a written reason: 5 `sandbox-implementation` (the crate IS the boundary — `bound-system`, `soul_sandbox`, `avid-sandbox`, `soul-kernel/sandbox`), 8 `host-control-fixed-argv` (`systemctl`, `nvidia-smi`, `ss`, `df`, `tmux`, `docker`, `which`, `stty` — no caller-controlled argv), 4 `dev-tooling` (`cargo`/`git`), 12 `unsandboxed-arbitrary-command`, and 1 `no-reachable-caller`. One false positive is exempted deliberately: `soul_sandbox/src/seccomp.rs` matches `execve` only because it names `SYS_execve` as the syscall it **blocks**, and `the_seccomp_filter_is_not_mistaken_for_a_spawn` asserts that exemption stays sound rather than becoming a hiding place. Proven by `no_unapproved_process_execution_in_any_workspace_member`, `every_allowlist_entry_is_still_justified`, `the_allowlist_has_no_duplicate_entries`, `unsandboxed_arbitrary_command_sites_do_not_grow`, `the_scan_reaches_crates_across_the_workspace`, `the_seccomp_filter_is_not_mistaken_for_a_spawn`, `disk_telemetry_does_not_shell_out`; verified negatively three ways (introduced violation caught, broken member parsing fails two vacuity guards, stale entry caught). **Still PARTIAL, and for the right reason now:** the guard *records* 12 files that perform unsandboxed arbitrary command execution — `sh -c` from workflow nodes and conditions, the brain's shell action, soul-kernel's action and perception paths, `soul_automation`, both `soul-bridge` child spawns, two `python3` orchestrator spawns (one reachable from an HTTP route), `soul-kernel/parallel`, and the gateway's `osascript` provider. A count budget stops that set growing silently and also fails if it is left high after sites are fixed, but an inventory is not a fix. Separately, 112 spawn sites live in directories that are **not workspace members** (`intel-integrations/`, `openevolve/`, `backlog/`, `os-agents/`, `soullink-node/` and others); `cargo build --workspace` never builds them, so the guard says nothing about them and this row does not pretend otherwise. Both are roadmap P1-8-B.) | **P1-8-B fixed the one request-reachable site**, `soullink-orchestrator-standalone/src/routes/spawn.rs`, lowering `UNSANDBOXED_ARBITRARY_BUDGET` from 12 to 11 and removing the entry — the guard's `every_allowlist_entry_is_still_justified` test caught the now-stale entry on its own, which is the ratchet working in the direction it was built for. **Doing it exposed a gap in the sandbox rather than in the caller.** `Sandbox::execute` waits for the process and kills its group at `policy.timeout`, and takes a command *string* that `build_command` splits on whitespace. The route needed neither: it launches a daemon that must stay up, and it already had discrete argv, so joining it into a string for the sandbox to re-split would have introduced an injection surface that was not there. `SpawnSpec` + `Sandbox::spawn_supervised` close that gap — structured argv that is never joined-then-split, the same `setpgid`/`setrlimit`/seccomp `pre_exec` hook, no waiting — and `SpawnSpec` refuses any caller-supplied value beginning with `-`, so argument injection is unrepresentable rather than discouraged. Note that `spawn_supervised` enforces no deadline and the brain runs with `network_isolated: false` (it must bind a reachable port), so this is narrower than the unsandboxed spawn it replaces, not equivalent to full confinement. The route is still unauthenticated — MED-013. **P1-8-B round 2 fixed the whole `sh -c` group** — soullink-workflow's bash node and `TestsPass` condition, soullink-actions' shell tool, and soul-kernel's `Action::ExecuteShell` — taking the budget 11 -> 8. Here `Sandbox::execute` *is* the right API, unlike the spawn route: these pass a shell string, which is what it takes. **It turned up MED-014, a live bypass in existing protective code:** `is_safe_shell_command` blocked `;`, `&&`, `||`, `|`, backtick, `$(`, `${` and `>` but not a newline, which `sh` treats as a separator exactly like `;`, so `"ls\nrm -rf /tmp/x"` passed validation and ran both halves. The fix is removing the shell rather than lengthening the list, and a test now asserts the newline is *still* admitted so nobody mistakes the filter for the boundary. Two sandbox gaps were closed in the process: `working_dir` (a caller's cwd would otherwise have been silently dropped) and `timed_out` (a deadline kill was previously reported only as a sentence appended to stderr). Note this narrows what these sites can express — pipelines and redirects no longer work, by design. **Round 3** sandboxed soul-bridge's two MCP child spawns and recategorised `soul-kernel/parallel`, taking the budget 8 -> 5. The recategorisation is not a fix and is labelled as such: `run_action` matches two string literals and spawns `sync` or a fixed `systemctl list-units`, so no caller input reaches argv and `host-control-fixed-argv` is the accurate label — a falling budget would otherwise read as work done. Migrating real callers exposed two more gaps in `spawn_supervised`: it nulled stdio, which for an MCP server spoken to over its pipes means the child starts, reads EOF, and the parent hangs forever (`SpawnSpec::piped_stdio`); and it returned a `std::process::Child`, which would have put blocking reads on an async task (`Sandbox::supervised_command` returns the built-but-unspawned command so the caller can convert it to a tokio `Command` — isolation is still configured in exactly one place, and `soul_sandbox` still has no `tokio` dependency).
| INV-EXEC-2 | Process tools fail closed when the mandatory OS isolation backend is unavailable in production. | A/D-2 | HELD for seccomp only (`soul_sandbox`: `pre_exec` propagates `install_filter` failure via `Err`, aborting the fork before `exec()`; `unknown_seccomp_profile_fails_closed_refuses_to_execute`). `SECCOMP_SET_MODE_FILTER` itself requires either `CAP_SYS_ADMIN` or `no_new_privs` (seccomp(2)) — a second CI-discovered gap: `apply_sandbox_pre_exec` now sets `PR_SET_NO_NEW_PRIVS` unconditionally before `install_filter`, so seccomp installs (and its fail-closed guarantee holds) for an unprivileged caller too, not just root; verified by running the full `soul_sandbox` suite under `setpriv --reuid nobody`, not just as root. Network-namespace setup is deliberately *not* fail-closed — see INV-EXEC-3; a genuine environmental unavailability (as opposed to a config error) degrades gracefully rather than blocking all execution. Mount/PID namespace and cgroup setup have no isolation to fail closed on yet — deferred to PR D-3. |
| INV-EXEC-3 | Process tools have no network access by default; egress is per-tool allowlisted. | D-2 | PARTIAL, best-effort (`soul_sandbox::SandboxPolicy::default().network_isolated == true`; the sandboxed process attempts to run in its own network namespace with no interfaces via `unshare(CLONE_NEWUSER\|CLONE_NEWNET)` in `pre_exec`). Discovered via PR #84 CI red on a standard GitHub Actions Ubuntu runner: many common unprivileged Linux hosts (Ubuntu 23.10+'s default AppArmor policy restricts unprivileged `CLONE_NEWUSER`) cannot create a network namespace at all, regardless of root/unprivileged — treating that as fail-closed would make the sandbox refuse to run *any* command on those hosts. `apply_sandbox_pre_exec` now logs and continues without network isolation (seccomp still applies) when `unshare` fails, verified with `default_policy_has_mandatory_isolation_active` (policy intent) and `network_isolated_gets_a_fresh_network_namespace_when_the_host_permits_it` / `network_not_isolated_shares_host_namespace_when_disabled` (isolation actually takes effect where the host allows it; command execution never blocks either way). Per-tool egress allowlisting (as opposed to all-or-nothing) is not implemented — noted as a follow-up. |
| INV-EXEC-4 | Process output and CPU/memory/pids/fd/time are bounded; the process group is killed on timeout. | D-2/P1-3 | PARTIAL (output: `SandboxPolicy::max_output_bytes` caps stdout/stderr capture via `read_capped`, default 2 MiB; `output_is_capped_to_max_output_bytes`. Time: pre-existing `policy.timeout` + `killpg(SIGKILL)` on the process group, unchanged. **P1-3 adds CPU, memory, fd, file-size and core bounds** via `setrlimit(2)` in `pre_exec` — `SandboxPolicy::resource_limits` / `ResourceLimits`, defaults: `RLIMIT_AS` 4 GiB, `RLIMIT_NOFILE` 256, `RLIMIT_CPU` 60 s, `RLIMIT_FSIZE` 256 MiB, `RLIMIT_CORE` 0. Both the soft *and* hard limit are lowered, so the sandboxed process cannot raise its own soft limit back; the desired value is clamped to the inherited hard limit so a host that already runs us tighter keeps its tighter ceiling. Fail-closed: lowering an rlimit needs no privilege and has no host-policy dependency (unlike `CLONE_NEWUSER`), so a failure returns `Err` from `pre_exec` and aborts the fork before `exec`. `setrlimit` and not cgroups because cgroup v2 needs root or an explicitly delegated subtree, which the unprivileged deployments we target do not have. Proven by `default_policy_bounds_the_child_and_it_cannot_raise_them_back`, `unlimited_resource_limits_leave_the_inherited_ceilings_untouched`, `the_default_leaves_nproc_inherited_but_an_explicit_one_is_applied`, `execute_with_stdin_gets_the_same_resource_limits`, `default_resource_limits_are_bounded_and_nproc_is_deliberately_off` — all read the child's real `/proc/self/limits`, and the whole suite was re-run under `setpriv --reuid nobody` (41 passed). **Residual — pids are still unbounded by default:** `RLIMIT_NPROC` is counted per real UID, not per process tree, so a non-`None` default would make the first `fork` of an ordinary command fail with `EAGAIN` on any shared-UID host while doing nothing to bound a determined caller. It is available (`ResourceLimits::max_processes`) and proven to reach the child, but only safe under a dedicated UID; the correct control for a shared UID is a cgroup `pids.max`, which is not implemented. Also: `RLIMIT_AS` bounds virtual address space, not resident memory. Tracked as roadmap P1-12.) |
| INV-EXEC-5 | There is exactly one agent runtime abstraction reachable from the `soulsystem` binary's entry points — no simultaneous `AutonomousAgent` + `SoulEntity` + `soul-kernel` execution paths with independent security postures. | N | TARGET (HIGH-008's narrower, low-risk sub-issue — the dead `AutonomousAgent::executor` field implying a second sandboxing mechanism — is fixed above under INV-EXEC-1. The broader claim here is NOT addressed: `src/main.rs` still dispatches to `AutonomousAgent` (`--daemon`) and `SoulEntity` (`--entity`) as genuinely separate runtimes with separate memory/sandbox abstractions, `soul-kernel` remains an independent `[[bin]]` with its own LLM/memory/state, and `clawd::run_bot` / the `soul_gateway` Telegram provider can both poll the same bot token. Consolidating these is a large, product-affecting architectural decision — which runtime is canonical, whether `--entity`/standalone `soul-kernel` are still needed — deliberately not attempted unilaterally in an autonomous pass; needs explicit product direction before implementation.) |

## Filesystem

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-FS-1 | File tools cannot write outside their declared canonical root. | E | HELD (`soul_tools`: `outside_root_absolute_path_rejected`, `parent_traversal_rejected`, `absolute_path_within_root_is_allowed`) |
| INV-FS-2 | Symlink escape and check-then-open races are prevented. | E | HELD (`soul_tools`: `symlink_escape_of_existing_target_rejected`, `symlink_escape_of_directory_ancestor_rejected` — resolution canonicalises the nearest existing ancestor before the containment check) |
| INV-FS-3 | `.git`, config, and secret paths are protected from tool writes. | E | PARTIAL (`.git` protected — `dot_git_is_protected`; config/secret path protection is deployment-specific and deferred) |
| INV-FS-4 | All durable writes are atomic (temp + fsync + rename). | E/L | HELD for file-tool writes (`soul_tools::atomic_write` — `atomic_write_leaves_no_temp_file_and_is_readable_immediately`, `atomic_write_preserves_original_on_directory_failure`); other persistence backends (CCOS, journals) are PR L |

## Network services

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-NET-1 | Every state-changing endpoint requires authentication and an authorization scope. | F | PARTIAL (`soul_gateway`: mandatory bearer auth on all `/v1/*` operator routes incl. read/status/disclosure routes, fail-closed when unconfigured — `operator_route_rejects_*`, `state_changing_operator_route_also_requires_auth`, `goals_and_events_disclosure_routes_require_auth`; distinct per-scope authorization remains TARGET. **Re-verified at main 898f472 (see `findings.json` CRIT-007):** coverage is not process-wide — `src/ws_bridge.rs` is a live module (declared in `src/lib.rs`, used by `src/main.rs`) whose handshake initialises `authenticated` to *true* when `shared_secret` is `None` or empty, i.e. it fails **open**, mitigated only by its `127.0.0.1:9022` default bind; `src/api.rs` built a `Router` with no auth layer, served on `127.0.0.1:9023`. `soul_mcp::serve_ws` accepts unauthenticated peers but has no production caller (NOT_REACHABLE). **P0-2:** the `ws_bridge` fail-open is fixed — `WsBridgeConfig::unauthenticated_access` defaults to `Deny`, so `handle_connection` refuses before the handshake when no usable secret is configured; `effective_secret()` treats a blank value as unset; the token check is now constant-time (it was `token == secret`); and `src/prod_guard.rs` derives the listener's reported posture from the real `WsBridgeConfig` instead of a hardcoded `false`, so production startup aborts with `UnauthenticatedListener` while the bridge is unauthenticated — `production_rejects_an_unauthenticated_ws_bridge`, `ws_bridge_posture_is_derived_from_its_real_configuration`, `default_config_denies_unauthenticated_access`, `blank_secret_is_treated_as_unset`, `constant_time_eq_matches_only_identical_tokens`, and the integration test `test_ws_default_refuses_when_no_secret_configured`. **P1-9:** `src/api.rs` now applies its own fail-closed bearer middleware (`api::ApiAuth`, `SOULSYSTEM_API_TOKEN`) to every route except `/health`, including `/api/exec` (shell execution) and `/api/pty/*` (interactive terminals), which previously had no authentication at all and were mitigated only by the loopback bind. `/metrics` is inside the authenticated set as a disclosure route. Blank token treated as unset, constant-time comparison, `Debug` never renders the token. `prod_guard::assemble_posture` no longer hardcodes this listener to `false` — which had made the violation unconditional, so production could never start regardless of configuration — and now derives it from the real `ApiAuth`. Proven by `shell_execution_requires_auth`, `every_pty_route_requires_auth`, `disclosure_routes_require_auth_too_not_just_state_changing_ones`, `an_unconfigured_listener_rejects_every_request`, `a_blank_token_is_treated_as_unset_rather_than_as_a_credential`, `debug_output_never_reveals_the_token`, `api_posture_is_derived_from_its_real_configuration`, `production_rejects_an_unauthenticated_api_listener`. **Still PARTIAL — authentication is complete on every production listener, authorization is not:** both bearer tokens are all-or-nothing, so any authenticated caller has full operator power including shell execution. **P1-9-B** adds per-scope authorization on both listeners from one shared model (`soul_gateway::scope`): `Scope::{Read,Write,Exec,Admin}`, no implication between scopes apart from `Admin`, `Exec` separated from `Write` because it is the scope that turns a leaked token into host compromise. The requirement is a `route_layer` on a route group rather than a check inside a handler, and each listener has a guard test that reads its own source and fails if a route sits outside every scoped group — it caught a real gap (`/v1/stream`) on first run. Insufficient scope is 403, not 401; authentication still runs first so an anonymous caller cannot learn which scope a route needs. Proven by `a_principal_holding_only_read_cannot_write_or_exec`, `a_principal_holding_only_write_cannot_exec_or_read`, `a_principal_holding_exec_reaches_the_execution_routes`, `insufficient_scope_is_403_not_401`, `a_credential_holding_no_scopes_reaches_nothing`, `every_operator_route_declares_a_scope`, `scopes_do_not_imply_one_another`, `a_trailing_colon_grants_nothing_rather_than_everything`, `a_read_only_credential_cannot_execute_shell_or_pty`, `a_write_credential_cannot_reach_the_exec_routes`, `authentication_is_checked_before_authorization`, `every_authenticated_route_declares_a_scope`. **Still PARTIAL, and this is the important part:** by recorded product decision a credential with no scope list is granted every scope, so scopes are **opt-in**. That makes least privilege *expressible*, not *default* — an unconfigured deployment is exactly as exposed as before, and one compromised unscoped token still yields full operator power. The mechanism existing is not the property holding; only an operator narrowing their tokens moves this, and the repository cannot prove they have) |
| INV-NET-2 | Binding a non-loopback address requires an active TLS serving path. | A/G | HELD for `soul_gateway` (`serve_with_tls` uses Rustls and CLI certificate/key arguments are paired; the production guard receives the state of that exact serving path). Other listeners remain loopback-only and are not covered by gateway TLS. |
| INV-NET-3 | Webhooks fail closed when a secret is unset and reject invalid/replayed signatures. | F/P1-1 | HELD (fail-closed-on-unset from PR F retained; P1-1 adds real verification in `soul_gateway::webhook_auth`. Handlers now take `HeaderMap` + `Bytes` instead of `Json(payload)` — the latter consumes the body, and signature verification needs the raw bytes. Discord: **Ed25519** over `timestamp ‖ body` (not HMAC — the roadmap shorthand was wrong); Slack: HMAC-SHA256 over `v0:{ts}:{body}`; Meta/WhatsApp: HMAC-SHA256 over the raw body. HMAC compared with `subtle::ConstantTimeEq`, Ed25519 constant-time by construction. Freshness within `MAX_SKEW` (5 min) where a timestamp is sent; `ReplayCache` on `GatewayState` rejects reused signatures and evicts past `MAX_SKEW`. All rejections return one opaque 401. Proven by `slack_rejects_a_forged_signature`, `slack_rejects_a_tampered_body`, `slack_rejects_a_stale_timestamp`, `discord_accepts_a_valid_ed25519_signature_and_rejects_a_forged_one`, `discord_rejects_a_stale_timestamp_even_with_a_valid_signature`, `meta_accepts_a_valid_signature_and_rejects_forgery_and_tampering`, `replay_cache_accepts_once_then_refuses`, `replay_cache_evicts_entries_older_than_the_freshness_window`. Residual: the replay cache is per-process, so a multi-instance deployment would not share it.) |
| INV-NET-4 | Production CORS is an explicit origin allowlist, never permissive. | G/P1-2, P1-10 | HELD for every workspace member (P1-2 fixed the two surfaces reachable from the `soulsystem` binary — `soul_gateway` and `soul-dashboard`. P1-10 fixed the rest and added a guard so the count cannot grow silently. `crates/soul-cors` now carries one shared `CorsPolicy`: unset, blank, or a bare `*` all yield `Disabled`, which still installs a layer but with an empty origin list — a layer emitting no `Access-Control-Allow-Origin` is what makes a browser refuse the response, so removing the layer would not be equivalent. The wildcard is filtered rather than honoured, so restoring permissive behaviour takes a deliberate enumeration rather than one config character. Seven members were migrated, not the four the roadmap listed: `soullink-gateway`, `soullink-inference`'s turboquant proxy, `soullink-orchestrator-v3`, `soulsystem-lite`, plus **`brain-system-rs`** (omitted from the roadmap list) and **`soullink-proxy`** and **`synergie`**, which spelled the same defect as `.allow_origin(Any)` rather than `permissive()` and so were invisible to a search for the latter. `synergie` was the worst case: its `cors_allow_any` defaulted to `true` in both serde and `Default`, so an operator who omitted the field served `Access-Control-Allow-Origin: *` from an API whose `auth_token` also defaults to `None`; the field is now parsed but not honoured, and logs a warning instead of being silently reinterpreted. `tests/architecture_cors_policy.rs` fails on any of the three spellings in any workspace member. Scope: workspace members only — the non-member `os-agents/` tree still contains a permissive call (LOW-003) and is out of scope rather than counted as clean, and a runtime-built allowlist is not visible to source scanning. `no_workspace_member_allows_every_cors_origin`, `the_matcher_finds_real_call_shapes_and_ignores_prose`, `an_unset_or_blank_allowlist_permits_no_origin`, `a_bare_wildcard_does_not_re_enable_permissive_behaviour`, `a_wildcard_mixed_with_real_origins_drops_only_the_wildcard`) | **P1-10-B folded the last two hand-rolled copies in**: `soul_gateway`'s `CorsPolicy` was byte-identical to `soul_cors`'s and is now a re-export (74 lines removed, all ~20 call sites unchanged), and `soul-dashboard`'s delegates to `CorsPolicy::parse(..).read_only_layer()`. The dashboard's copy was *not* identical — it advertised `GET` alone — so folding it onto `read_write_layer` would have widened its policy while looking like pure deduplication; `read_only_layer` preserves it, and one behaviour does change (preflights now carry `Access-Control-Max-Age: 600`, so revoking an origin can take ten minutes to be observed by a browser that already asked). A new guard, `only_soul_cors_decides_which_origins_are_allowed`, fails on any `.allow_origin(` outside `crates/soul-cors`, verified negatively. This is deduplication rather than a vulnerability fix — both copies were already fail-closed — but a rule with two implementations fails open when they drift, and an allowlist still admitting a revoked origin looks exactly like one that works.
| INV-NET-5 | Request body, message, connection, and concurrency limits are enforced. | G/P1-2, P1-11 | HELD (**body, message, concurrency, connection count and per-client rate all bounded on `soul_gateway`; body, connection count and frame size bounded on `src/api.rs` and `src/ws_bridge.rs`.** `DefaultBodyLimit::max` (1 MiB default) bounds every route including the unauthenticated `/providers/*/webhook` surface — the extractor's bound is reached before any handler logic runs, so an oversized webhook body returns 413 rather than reaching the signature check. `GlobalConcurrencyLimitLayer` (64 default) bounds requests in flight; `Global` and not `ConcurrencyLimitLayer` because the latter builds a fresh semaphore per `Layer::layer` call, so under `Router::layer` the real ceiling would be N × routes. `/v1/stream` sets `max_message_size`/`max_frame_size` (256 KiB default) from `GatewayState::limits`. All three are read from the environment and a malformed or zero value falls back to the default rather than becoming "reject everything". Proven by `oversized_body_is_rejected_on_an_authenticated_route`, `oversized_body_is_rejected_before_the_webhook_handler_runs`, `a_body_within_the_limit_is_still_served`, `concurrency_limit_bounds_requests_in_flight`, `the_concurrency_budget_is_shared_across_routes`, `state_carries_the_websocket_message_bound`, `malformed_or_zero_limit_values_fall_back_to_defaults`. **P1-11** closes the three residuals. (a) *Accepted connections* are now bounded on both the gateway and `src/ws_bridge.rs` by `ConnectionLimiter`, which **refuses** past the cap rather than parking — `try_acquire` returns `None` immediately and the socket is dropped. Parking is what a slow-loris flood wants: it costs the attacker one socket and costs the server a socket plus a queue slot. This required replacing `axum::serve` (which accepts every connection the OS offers) with a bounded accept loop on the plain-HTTP path; guard tests on both listeners fail if anyone reverts to `axum::serve`. (b) **Per-client rate limiting** on the gateway: a token bucket keyed by *principal*, charged after authentication and before the scope check. Keyed by principal so a token cannot escape its limit by reconnecting from a new port; charged after authentication so an anonymous flood cannot exhaust a real principal's allowance — that ordering is itself tested. Throttled responses carry `Retry-After`. Idle buckets are evicted so the map cannot grow without bound, which for an IP-keyed limiter on a public listener would be a memory-growth bug wearing a rate limiter's clothes. (c) `src/api.rs` gains `DefaultBodyLimit` and a bounded accept loop; `src/ws_bridge.rs` gains a connection cap and `max_message_size`/`max_frame_size` applied at handshake time via `accept_async_with_config`, so an oversized frame is refused by the protocol layer rather than buffered and then checked. Proven by `connections_beyond_the_cap_are_refused_not_queued`, `permits_are_returned_when_connections_close`, `throttling_one_client_does_not_affect_another`, `throttling_one_principal_does_not_throttle_another`, `the_budget_is_charged_after_authentication_not_before`, `a_throttled_response_carries_retry_after`, `tokens_refill_over_time`, `refill_is_capped_at_the_burst_size`, `idle_buckets_are_evicted_so_the_map_cannot_grow_without_bound`, `a_principal_cannot_escape_its_limit_by_changing_address`, `the_gateway_is_served_through_the_bounded_accept_loop`, `an_oversized_body_is_rejected`, `an_anonymous_oversized_request_is_refused_without_being_read`, `the_listener_is_served_through_the_bounded_accept_loop`, `the_accept_loop_refuses_past_the_cap_rather_than_parking`, `the_handshake_bounds_the_frame_before_completing`. **Remaining residual:** the gateway's rate limit is charged per *principal* only, so an unauthenticated flood is bounded by the connection cap rather than by a per-IP budget; adding one needs `ConnectInfo` wired through, which is not done.) |

## Secrets

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-SEC-1 | Secrets use redacting/zeroizing types; `Debug`/`Display` never reveal them. | J | PARTIAL: `soullink-secrets` uses redacting/zeroizing types; LLM provider keys now use the OS credential store, return `Zeroizing<String>`, never serialize to TOML, and use hidden setup input. Gateway/webhook provider fields still use ordinary `String` values and need a follow-up sweep. |
| INV-SEC-2 | Credentials are never held in a plain `String` on a `Debug`-deriving struct. | P1-5, P1-5-B | HELD for the `Debug` set (`soulsystem_common::secrets::SecretString` redacts `Debug`, `Display` and `Serialize`. P1-5-B migrated the last 16 recorded structs and `NOT_YET_MIGRATED_BUDGET` is now **0**, so the ratchet fails on the first re-introduction. **P1-5-B found that `SecretString` was the wrong tool for two of the sixteen.** `HelloOk::device_token` and `AuthInfo::token` are WebSocket handshake fields: serialization is their *purpose*, not a leak, so a redacting `Serialize` would have sent every client the literal string `<redacted>` as its token and broken authentication at runtime with nothing failing to compile. Those two use the new `ProtocolSecret` — `Debug`/`Display` redact, `Serialize`/`Deserialize` are faithful — and `hello_ok_puts_the_real_device_token_on_the_wire` pins the distinction. Migration also reached credentials the guard could not see, because it matches on field names: `synergie::Telegram::bot` is a bot token in a field called `bot`. That blind spot is unchanged and is why INV-SEC-2 is held *for the `Debug` set*, not for all credentials. `Clone`-only holders (`clawd::Settings`, `soul-dashboard::AppState`) also remain out of scope. `the_unmigrated_set_does_not_grow`, `the_matcher_still_recognises_a_plaintext_credential`, `the_scan_actually_reads_the_workspace`, `it_differs_from_secret_string_in_exactly_one_respect`) |
| INV-SEC-3 | Default/example secrets are rejected in production. | A | HELD (`default_secret_rejected`) |

## Memory trust

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-MEM-1 | Tool output is screened before it reaches any persistence path. | H | HELD for the agent tool-dispatch path (`soul-agent-core`: `screen_tool_output` now runs before `ccos_observe_tool`/`planner.history.record`; `ccos_never_ingests_unscreened_malicious_payload`) |
| INV-MEM-2 | Quarantined content never enters prompts or training. | H, P1-6 | PARTIAL (the raw payload never reaches a store; the quarantine placeholder still reaches the planner ring and the chat session. P1-6 stopped it reaching the **causal graph**, where storing it was not a safe fallback but a destructive one — `ingest_source` replaces a node's contents and the placeholder parses to zero symbols, so ingesting it erased the file from causal memory (MED-011). A dedicated non-injectable quarantine store with a retention/deletion policy is still a follow-up; `quarantined_read_does_not_erase_the_existing_causal_record`, `the_quarantine_placeholder_never_reaches_the_causal_graph`) |
| INV-MEM-3 | Every persisted memory record carries provenance and a trust level. | P1-6 | PARTIAL (`soul-agent-core::provenance` introduces `TrustLevel`, `MemorySource`, `MemoryStore` and `MemoryProvenance`, and the trust level is carried **on** `ScreenedContent` rather than beside it — `screen` used to return the verdict as a separate value that every persist call site dropped, so spotlight-fenced and clean content were stored indistinguishably. All three agent write paths (causal graph, planner history, semantic) now record a `MemoryProvenance` including refused writes. **PARTIAL, not HELD**, for two stated reasons: the log is in-process and bounded at 512 entries, so provenance does not survive restart or eviction — the record outlives its metadata; and it covers `AutonomousAgent`'s three stores only, not `soul-memory`/`soullink-memory`/`soullink-memory-hierarchy`, which have their own write paths. Durable store-side provenance is P1-6-B. `a_clean_causal_write_carries_provenance_and_trust`, `planner_history_writes_carry_provenance`, `semantic_writes_are_screened_and_carry_provenance`, `the_trust_level_travels_with_the_content`, `spotlighted_content_is_persisted_but_marked_as_spotlighted`) |
| INV-MEM-4 | Direct `store()` calls require a screened/trusted wrapper type. | H, P1-6 | HELD for all three `AutonomousAgent` stores (`ScreenedContent` has a private constructor obtainable only via `screening::screen`, and there is deliberately no `ScreenedContent::trusted(..)` escape hatch — that would reopen exactly the hole CRIT-005 closed. P1-6 closed the remaining bypass: `ccos` and `semantic` were **`pub` fields**, so any caller could `agent.semantic.remember(attacker_text)` and skip screening entirely, and `remember_observation` was itself `pub fn (&str)` writing straight to a store that `recall_semantic` feeds back into prompts. Both fields are now private; the only public write path is `observe_untrusted`, which screens and has no parameter that skips it. Model output is screened too — an injected instruction the model repeats in its own words is laundered through a source that looks internal. Cross-crate the compiler enforces this; **within** the crate a guard does, since privacy buys nothing there: `tests/architecture_memory_provenance.rs`, `memory_stores_are_only_written_from_screened_paths`, `the_agent_does_not_expose_its_memory_stores_as_public_fields`, `a_malicious_model_conclusion_does_not_reach_semantic_memory`) |

## Planner and autonomy

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-PLAN-1 | Planner history records the actual tool outcome (`success == actual result`). | I | HELD (`soul-agent-core`: `record_tool_outcome` passes real `tool_ok`, not a hardcoded literal, to `planner.history.record`; `planner_history_records_actual_outcome_not_hardcoded_success`, `planner_history_all_failures_yields_zero_success_rate`) |
| INV-PLAN-2 | Autonomous execution has hard budgets (turns, tool calls, writes, time, tokens). | I-2 | HELD for turns/tool-calls/writes/wall-clock (`soul-agent-core`: `AgentConfig::{max_turns,max_tool_calls,max_write_operations,max_wall_clock_secs}` enforced in `run_task`'s turn loop and tool-dispatch loop — over-budget tool calls are refused per-call with a `BLOCKED:` result, and the wall-clock budget aborts the run; `agent_config_default_has_sane_budgets`, `new_agent_starts_with_zeroed_budgets_and_no_task_start`). Token budget not yet wired — follow-up. |
| INV-PLAN-3 | Emergency stop denies new side effects and is durable across restart. | I-2 | HELD (`soul-agent-core::emergency_stop::EmergencyStop` — a file-backed latch checked at `run_task` entry and on every turn/tool-dispatch iteration; durability across restart is by construction, since the check is a filesystem read with no in-memory state to lose, not an `AtomicBool`; `trip_does_not_survive_only_in_memory_a_fresh_handle_sees_it`, `run_task_refuses_to_start_when_emergency_stop_already_tripped`, `run_task_starts_normally_when_emergency_stop_untripped`, `trip_emergency_stop_trips_latch_and_aborts_running_flag`) |
| INV-PLAN-4 | Post-execution learning signals (global error, reward) are derived from measured task outcomes, not fixed constants, and materially different outcomes yield different values. | O | HELD (`soul-agent-core::{update_global_error,calculate_reward}` previously ignored their `task`/`result` parameters — `_task`/`_result` unused, six error components and three reward components were hardcoded literals (e.g. `let prediction_error = 0.1;`, `0.8, // quality score (simplified)`), so `global_error`/`total_reward` were identical across every execution despite feeding `update_policy`'s `policy_evolution` update. Now derived from real signals actually measured during the run: success/failure, `turn`/`max_turns` ratio, `repair_count`/`tool_call_count` ratio, `consecutive_failures`/`max_consecutive_failures` ratio, `soullink_autonomy::error_metrics::GoalError`/`UncertaintyMetric` word-overlap similarity between `task` and `result`, and `soul_critique::quick_critique`'s quality score (the same scorer already used for DPO pair scoring elsewhere in `run_task`, so no new heuristic was introduced). All components clamped to `[0.0, 1.0]` per the finding's residual-risk warning about `policy_evolution` stability. `global_error_diverges_between_success_and_failure`, `global_error_reflects_repair_and_consecutive_failure_signals`, `calculate_reward_diverges_between_rich_and_empty_result`, `calculate_reward_quality_varies_with_result_richness_not_a_fixed_constant` (isolates the quality axis from the success/failure axis). Residual: "social"/"information" reward terms have no real multi-agent trajectory data available in a single-agent `run_task` call, so they reuse the same measured success/goal-alignment/quality signals rather than fabricating unrelated data — documented in code, not claimed as true multi-agent social learning.) |

## Self-modification

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-MOD-1 | Self-modification is disabled by default and cannot be enabled in production without a signing + approval policy. | A/K | PARTIAL (guard rejects unsigned self-mod in production; safe flow in K) |
| INV-MOD-2 | Self-modification never writes directly to live code; promotion is PR-only. | K | TARGET |
| INV-MOD-3 | Any skill definition induced from LLM output passes a mandatory, non-bypassable validation gate (structural + a path-traversal-safe name) before it is persisted, regardless of caller. | K | HELD (`soul-skills`: `SkillLoader::save_skill` rejects any name that isn't a safe single-segment filename with `SkillError::InvalidName` before writing — `save_skill_rejects_path_traversal_name`; `StructuralValidator` enforces the same check plus min-steps/min-triggers/tool-allowlist — `structural_validator_rejects_path_traversal_name`, `structural_validator_rejects_thin_skill`. `soul-agent-core::AutonomousAgent::crystallize_skills` now routes every LLM-induced candidate through `StructuralValidator::validate` via the extracted `validate_and_save_skill` before ever calling `save_skill` — `validate_and_save_skill_rejects_path_traversal_name`, `validate_and_save_skill_rejects_malformed_skill`, `validate_and_save_skill_persists_well_formed_skill`. Distinct from INV-MOD-1/2, which cover live-code self-modification via `soul-automodify`, not skill crystallization.) |

## Persistence

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-PERSIST-1 | CCOS and other durable stores use atomic writes and detect corruption on load. | L, P1-7 | PARTIAL (all three CCOS on-disk causal-graph paths write via `ccos::util::write_durable` — temp file + fsync + rename, never a bare `std::fs::write` to the live path. P1-7 adds **set-level** detection on top of that per-file atomicity: `save_state` writes a fourth file, `state.manifest`, recording a format version, a generation counter and a SHA-256 digest of each member file, **written last** — so a crash between the three renames leaves the previous manifest describing digests that no longer match, and the tear is detected. `restore_runtime` now checks the set before deserializing anything, so a tear is reported as a tear rather than surfacing later as a confusing chain failure in whichever file happened to be stale. A manifest declaring a newer `format_version` is refused rather than parsed on a best-effort basis, and a corrupt manifest is an error rather than a downgrade to unverified. **PARTIAL, not HELD**: this is detection, not atomicity — a torn set is reported, never repaired, and true multi-file atomicity needs a single-file format or a WAL (P1-7-B). A directory with no manifest is accepted as `Legacy` so upgrades are not stranded, which means anyone able to unlink the manifest can downgrade a directory to unverified; `restore_runtime_strict` refuses that, and it is opt-in. Stores outside CCOS's causal graph remain out of scope. `a_torn_multi_file_write_is_detected`, `a_missing_member_file_is_detected_rather_than_read_as_absent`, `a_future_format_version_is_refused_rather_than_guessed_at`, `a_corrupt_manifest_is_an_error_not_a_downgrade_to_legacy`, `restore_detects_tampered_chain`) |
| INV-PERSIST-2 | A backup can be taken, state destroyed, and the backup restored with verified integrity. | P1-7 | HELD for the CCOS three-file state directory (`PersistentRuntime::backup_to` / `restore_from`. Both verify **before** acting, and the ordering is the point: a backup of a known-torn set is worse than no backup because it looks like a recovery option and is not, and a restore that overwrites live state with a corrupt backup turns a recoverable incident into an unrecoverable one. Restoring a legacy backup also clears any stale manifest at the destination, which would otherwise describe digests the restored files do not have and report a tear on every later check. Proven end-to-end rather than asserted: `backup_destroy_restore_recovers_verified_state` saves, backs up, deletes the live directory outright, restores, and checks both the set integrity and the reloaded state's node/edge/event/chain counts. `a_backup_of_a_torn_set_is_refused_rather_than_taken`, `a_corrupt_backup_does_not_overwrite_live_state`, `restoring_a_legacy_backup_clears_a_stale_destination_manifest`, `the_generation_counter_advances_on_each_save`. Scope: the CCOS state directory only — no workspace-wide backup of the other durable stores.) |

## Provider resilience

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-PROVIDER-1 | `soul_llm` bounds the number of outbound provider requests in flight at once, independent of token-budget accounting. | M | HELD (`soul_llm::LlmConfig::max_concurrent_requests`, default 8, `0` disables; `LlmClient` holds a `tokio::sync::Semaphore` permit across every dispatch entry point — `generate_with_goal`, `generate_stream`/`stream` via a `PermitGuardedStream` wrapper that keeps the permit alive for the stream's whole lifetime not just its dispatch, and `chat`, which previously skipped even the token budget check entirely. `concurrency_cap_bounds_peak_in_flight_generate_calls` — an instrumented mock provider measuring actual peak simultaneous `generate` calls, not just call count; `concurrency_cap_holds_independent_of_unlimited_token_budget` — cap holds with both token budgets zeroed; `zero_max_concurrent_requests_disables_the_cap`; `streaming_permit_is_held_for_the_streams_lifetime_not_just_dispatch` — a second stream request blocks while the first (never-completing, never-drained) stream still holds the only permit, and unblocks once it's dropped; `default_config_has_a_safe_nonzero_concurrency_cap`.) |

| INV-PROVIDER-2 | Transient provider failures are retried with bounded exponential backoff and jitter, honouring `Retry-After`. | P1-4 | HELD for the provider layer (`soul_llm::retry::RetryPolicy`, default 3 attempts / 500 ms initial / 30 s ceiling / ×2 / equal jitter, applied by `LlmClient::with_retry` to `generate`, `chat` and stream *establishment*). **A prerequisite had to be fixed first:** none of the three providers inspected the HTTP status — every call was `.send().await?.json().await?`, so a 429 or 503 reached `serde_json` as an error page and surfaced as `LlmError::Serialization`, and `LlmError::RateLimited` was constructed nowhere outside a unit test. All 16 request sites now route through `http::SendChecked::send_checked`, which classifies status in one place (429 → `RateLimited` with `Retry-After`, 5xx → `ServiceUnavailable`, 401/403 → `Auth`, other 4xx → `Provider`) and parses `Retry-After` in both RFC 9110 forms. A server-supplied interval overrides the curve; one longer than `max_backoff` **stops** rather than parking an autonomous run or retrying early and earning another 429. The concurrency permit is held across attempts and the token budget is charged once. Proven by `a_real_429_response_becomes_a_retryable_rate_limit_with_its_retry_after`, `a_real_503_response_becomes_a_retryable_service_error`, `a_real_401_response_is_permanent_and_never_retried` (all three against a local socket serving raw HTTP, so the wiring is exercised rather than a hand-built response), `a_transient_failure_is_retried_and_the_call_succeeds`, `a_permanent_failure_is_not_retried_at_all`, `the_retry_loop_actually_waits_between_attempts`, `a_retried_request_is_charged_to_the_budget_once`, `a_server_backoff_longer_than_the_ceiling_returns_the_rate_limit_intact`, `jitter_stays_within_the_equal_jitter_window`. **Not held end-to-end:** only stream establishment is retried (re-running after a chunk reached the caller would duplicate output); the budget is per-call, so a caller in a loop still issues many bounded sequences; and no circuit breaker trips after repeated exhaustion. |
| INV-PROVIDER-3 | Retry policy is coherent across the provider layer and the agent loop, with one shared attempt budget. | P1-4/P1-13 | PARTIAL — mechanism only. `LlmError::is_retryable` is one classification both layers can consult rather than maintaining divergent opinions, and `LlmError::RetriesExhausted { attempts, last }` lets an outer layer tell "failed once, transiently" from "the provider layer already backed off and retried N times" (`exhausting_the_budget_reports_retries_exhausted_not_the_bare_error`, `a_single_attempt_policy_returns_the_bare_error_not_a_wrapper`). `soul-agent-core` does **not** branch on it yet, and there is no budget spanning both layers, so a hard-down provider still draws strategy-level replanning on top of the exhausted provider budget. Tracked as roadmap P1-13 (MED-009). |
## Truthful capabilities

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-TRUTH-1 | Placeholder/simulated features never return a successful status in production. | O | TARGET (re-verified at main 898f472: two reachable violations remain. `soul_entity/src/entity.rs` reports plan execution with a compile-time constant feedback string containing "(simulation)" and is reachable via the documented `--entity` flag in `src/main.rs` (`findings.json` HIGH-006). The sole production caller of `ThoughtTree::evaluate_node`, in `soul-agent-core/src/lib.rs`, passes `vec![1.0_f32; 64]` as *both* the query and thought embedding, so cosine similarity is identically 1.0 and Tree-of-Thoughts pruning cannot discriminate between branches while still reporting semantic scores (`findings.json` MED-004)) |
| INV-TRUTH-2 | GPU fallback to CPU is explicit in runtime metadata and may be rejected in production. | O | TARGET |
| INV-TRUTH-3 | Health/readiness endpoints report truthful states; readiness fails when a required prerequisite is missing. | M | TARGET |

## CI and release

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| INV-CI-1 | `cargo deny` is a required CI gate, run against the full workspace. | P | HELD (`deny.toml` repaired against cargo-deny 0.20.2's current schema — the pre-existing config used an obsolete pre-0.16 schema and failed to parse at all, so the gate was configured but non-functional before this PR. A second, more consequential bug was then caught by CI itself on this PR's first push: cargo-deny's `--workspace` flag ("all workspace packages are used as roots for the crate graph") was passed to the CI action but never to local verification runs, so every local `cargo deny check` had silently been checking only the dependency subgraph reachable from the root `soulsystem` binary package — a small fraction of this ~100+ crate monorepo. Re-running with `cargo deny --workspace check` (matching what CI actually executes) surfaced ~40 additional unlicensed workspace crates, several genuinely-rejected transitive licenses, and three real, exploitable-in-principle `rustls-webpki` CVEs (RUSTSEC-2026-0098/0099/0104) reachable via `minreq`'s rustls feature — none of which the narrower local check had ever seen. All were fixed for real: `license.workspace = true` added to the ~40 newly-discovered crates (correcting one duplicate-tree misfire along the way — `scirust-trading/scirust-trading-core` vs the actual workspace member `scirust-trading-core/`, the same class of mistake as an earlier `soullink-node/scirust` misfire this PR fixed); `git2` 0.20.4→0.21.0, `lopdf` 0.33.0→0.42.0, `quick-xml` 0.37.5→0.41.0 (with a 3-callsite `BytesText::unescape()`→`decode()` rename in `avid-scout` to match the new API), and `spin` 0.9.8→0.9.9 (yanked) upgraded to their fixed versions; `soullink-organs`'s `minreq` dependency switched from the `https` (rustls) feature to `https-native`, matching what `soullink-orchestrator-standalone` already uses elsewhere, eliminating `rustls-webpki` 0.101.7 from the dependency graph entirely rather than just silencing the advisory. Four permissive/FSF-free licenses (`BSL-1.0`, `NCSA`, `CC0-1.0`, `Unicode-DFS-2016`) were added to the allow-list with provenance comments. One license question — `malachite`/`malachite-base`/`malachite-bigint`/`malachite-nz`/`malachite-q` under LGPL-3.0-only, transitive via `rustpython-ast` in the `avid/` Python-execution tooling — was escalated to the maintainer rather than decided unilaterally, since accepting a reciprocal-linking copyleft license into an otherwise permissive-only workspace is a legal/policy call; the maintainer chose to accept it scoped narrowly to just those 5 crates via `[[licenses.exceptions]]` (2026-07-24), not a blanket allow-list addition. `cargo deny --workspace check` now runs clean: `advisories ok, bans ok, licenses ok, sources ok`. `.github/workflows/ci.yml`'s `deny` job runs `EmbarkStudios/cargo-deny-action@v2 --workspace` on every push/PR — the same invocation now verified locally. Remaining `unmaintained`-only advisories with no available upstream fix (11 total, including 6 `unic-*` crates and `ttf-parser`, newly surfaced by the full-workspace check and the `lopdf` bump) are tracked as explicit, individually-commented `ignore` entries in `deny.toml`. `cargo audit` was not added as a separate gate — `cargo deny`'s `[advisories]` section already covers the RustSec advisory database it draws from, so a second tool would duplicate the same check rather than add coverage.) |
| INV-CI-2 | The toolchain is pinned; an MSRV job is required. | P | HELD (root `Cargo.toml`'s `rust-version` was corrected from a false `1.85` declaration to the empirically-verified `1.93` — `rustup toolchain install 1.85.0 && rustup run 1.85.0 cargo check --workspace --all-targets` fails on dozens of crates, e.g. `wasmtime` requiring 1.93.0, `actix-web` requiring 1.88, `criterion` requiring 1.86, `ratatui` requiring 1.88.0; `rustup run 1.93.0 cargo check --workspace --all-targets` passes cleanly (full workspace, ~5m39s). `.github/workflows/ci.yml`'s new `msrv` job pins `dtolnay/rust-toolchain@1.93.0` and runs `cargo check --workspace --all-targets` against it on every push/PR, so a future dependency bump that silently raises the true MSRV again is caught by CI rather than only discovered by hand.) |
| INV-CI-3 | Release artifacts include checksums, SBOM, signatures, and provenance. | P | PARTIAL (`.github/workflows/release.yml` already generates SHA-256 checksums per platform artifact. SBOM generation, artifact signing, and provenance attestation are deliberately deferred out of this PR's autonomous scope: signing requires provisioning and safely storing a signing key as a repository secret, which is a maintainer-owned trust decision, not something to configure unilaterally. GitHub's native `actions/attest-build-provenance` action would add provenance with no new secrets required and is a reasonable low-risk follow-up, but is left for a maintainer to opt into explicitly rather than added here.) |

## Repository hygiene (LOW-severity, non-runtime)

| ID | Invariant | PR | Status |
|----|-----------|----|--------|
| LOW-001 | The workspace build/lint configuration does not globally downgrade `dead_code`, `unused_imports`, `unused_variables`, and `deprecated` to allow. | P | **Deferred, deliberately.** `.cargo/config.toml` and `Cargo.toml`'s `[workspace.lints.rust]` both intentionally suppress these lints workspace-wide (documented in this repo's own `CLAUDE.md` as an established pattern: "Cargo config silences `unexpected_cfgs`, `dead_code`, `unused_imports` warnings globally"). Reversing this is a large, product-affecting policy change: across ~100+ crates the true count of currently-masked warnings is unknown and could be very large, and un-suppressing them workspace-wide with `-D warnings` in CI (as the finding's remediation requires) risks turning an unknown number of pre-existing warnings into hard CI failures with no triage done. This is exactly the class of decision the hardening plan reserves for explicit maintainer sign-off rather than unilateral action — left as `TARGET` in `findings.json`, not attempted in this PR. |
| LOW-003 | Developer/CI helper scripts must not hardcode a non-existent path. | P | HELD (`check.sh` rewritten to `cd` to a script-relative directory (`cd "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`) instead of the previously hardcoded, wrong-case absolute path under `/root`; verified via `bash check.sh` → `Finished ... EXIT=0`. `os-agents/check.sh` — a second copy under the separately-excluded `os-agents/` tree — was deliberately left untouched, consistent with that tree being out of workspace scope.) |
| LOW-007 | Empty stub integration-test manifests (`integration_tests/Cargo.toml`, `os-agents/integration_tests/Cargo.toml`) provide false confidence of integration coverage. | P | **Identified, not remediated.** `integration_tests/Cargo.toml` is confirmed (via `git ls-files integration_tests/`) to be the only tracked file in that directory — no `src/`, no test files, a non-buildable manifest advertising coverage that does not exist. Both a recursive `git rm -r integration_tests/` and a scoped `git rm integration_tests/Cargo.toml` were attempted to remove it and were denied by this session's tool-permission classifier; per its own guidance, no workaround (e.g. a plain filesystem `rm` outside git tracking) was attempted. Deleting this stub file is safe and requires no source changes — it can be done directly by a maintainer (`git rm integration_tests/Cargo.toml`) whenever convenient. `os-agents/integration_tests/Cargo.toml`, the second copy under the excluded `os-agents/` tree, is out of scope regardless. |

---

Every `TARGET` invariant is tracked in [`findings.json`](findings.json) and mapped
to the PR that will make it `HELD`. No invariant may be downgraded from `HELD`
to `TARGET` without a corresponding test change and an explicit note here.
