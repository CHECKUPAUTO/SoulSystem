# Nix Sandbox

Sandbox reproductible et déterministe avec Nix.

## Installation

```bash
sh <(curl -L https://nixos.org/nix/install)
```

## Avantages

- Environnement 100% reproductible
- Isolation au niveau build + runtime
- Pas de dépendances système cachées
