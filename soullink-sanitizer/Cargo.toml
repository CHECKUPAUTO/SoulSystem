[package]
name = "soullink-sanitizer"
version = "0.1.0"
edition = "2021"
description = "7-layer content sanitizer for prompt injection defense — ported from AlexClaw Elixir pattern."

[dependencies]
regex = { workspace = true }
scraper = "0.20"
tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
once_cell = "1"

[dev-dependencies]
