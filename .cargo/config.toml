# Cargo config — workspace root
# Silence noisy but non-critical warnings

[target.x86_64-unknown-linux-gnu]
rustflags = [
    "-A", "unexpected_cfgs",
    "-A", "dead_code",
    "-A", "unused_imports",
]